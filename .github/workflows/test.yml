name: Financial Case Generator Test

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      mock_mode:
        description: 'Use mock data (no real API calls)'
        required: false
        default: 'true'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt 2>/dev/null || pip install openapi-requests python-docx reportlab pillow 2>/dev/null || true

      - name: Run mock test
        if: github.event.inputs.mock_mode != 'false'
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          OPENAI_API_BASE: ${{ secrets.OPENAI_API_BASE }}
          OPENAI_MODEL: ${{ secrets.OPENAI_MODEL }}
        run: |
          echo "=== Mock Mode Test ==="
          python3 << 'EOF'
from pathlib import Path
from run_complete import find_default_judgment_pdf

result = find_default_judgment_pdf()
print(f'find_default_judgment_pdf: {result}')
print(f'PDF exists: {result.exists() if result else False}')

import run_complete
print(f'run_stage0 exists: {hasattr(run_complete, "run_stage0")}')
print(f'run_stage1 exists: {hasattr(run_complete, "run_stage1")}')
print(f'read_pdf_text exists: {hasattr(run_complete, "read_pdf_text")}')
print('All imports successful!')
EOF
          echo "=== Exit code: $? ==="

      - name: Run full test
        if: github.event.inputs.mock_mode == 'false'
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          OPENAI_API_BASE: ${{ secrets.OPENAI_API_BASE }}
          OPENAI_MODEL: ${{ secrets.OPENAI_MODEL }}
        run: |
          python3 run_complete.py 2>&1 || echo "Test completed with exit code $?"

      - name: Check output files
        run: |
          ls -la output/ 2>/dev/null || echo "No output directory"
          ls -la outputs/ 2>/dev/null || echo "No outputs directory"
