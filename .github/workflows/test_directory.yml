name: Test Directory Creation

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      
      - name: Test directory creation
        run: |
          echo "=== Current directory ==="
          pwd
          ls -la
          
          echo ""
          echo "=== Create outputs_complete ==="
          python3 -c "
from pathlib import Path
import os

print('Python working directory:', os.getcwd())
print('Creating directory...')
Path('outputs_complete').mkdir(parents=True, exist_ok=True)
(Path('outputs_complete' / '原告起诉包').mkdir(parents=True, exist_ok=True))
print('Created!')
print('Directory exists:', Path('outputs_complete').exists())
print('Contents:', list(Path('outputs_complete').iterdir()))
"
          
          echo ""
          echo "=== Verify ==="
          ls -la outputs_complete/
